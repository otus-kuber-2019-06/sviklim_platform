apiVersion: v1
kind: Pod
metadata:
  name: storage-pod
spec:
  containers:
    - name: my-frontend
      image: busybox
      volumeMounts:
        - mountPath: "/data"
          name: storage-volume
      command: ["sleep", "1000000"]
      resources:
        requests:
          memory: "32Mi"
          cpu: "128m"
        limits:
          memory: "64Mi"
          cpu: "256m"
  volumes:
    - name: storage-volume
      persistentVolumeClaim:
        claimName: storage-pvc
